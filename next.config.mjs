/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow cross-origin requests from the preview tool (127.0.0.1 ↔ localhost)
  allowedDevOrigins: ["127.0.0.1", "localhost"],
};

export default nextConfig;
